# Задачи по теме: Взаимодействие PHP и MySQL (продолжение) (20 вариантов)

## Вариант 1
Получите все строки из таблицы users (или любой) через $stmt->fetchAll(PDO::FETCH_ASSOC). Выведите в цикле foreach каждую строку (например, id и name). Если таблицы нет — симулируйте массив и выведите его.

## Вариант 2
Реализуйте пагинацию: $page = (int)max(1, $_GET['page'] ?? 1), $perPage = 10, $offset = ($page - 1) * $perPage. Запрос SELECT ... LIMIT :limit OFFSET :offset. bindValue с PDO::PARAM_INT для limit и offset. Выведите массив страницы и номера страницы. Симулируйте GET.

## Вариант 3
В mysqli получите результат как объекты: $result->fetch_object(). В цикле выведите $row->id, $row->name. Продемонстрируйте на одном запросе (или симулируйте массив объектов).

## Вариант 4
Напишите функцию getUserById(PDO $pdo, int $id): ?array. Выполните SELECT * FROM users WHERE id = ?. fetch(). Верните массив или null. Вызовите для id=1 и выведите результат или «Пользователь не найден».

## Вариант 5
fetchAll с режимом PDO::FETCH_COLUMN: получить один столбец (например, имена). SELECT name FROM users. fetchAll(PDO::FETCH_COLUMN). Выведите массив имён. Симулируйте при отсутствии таблицы.

## Вариант 6
Подсчёт общего количества записей для пагинации: SELECT COUNT(*) FROM users. fetchColumn(). Выведите «Всего записей: N». Вычислите $totalPages = ceil($total / $perPage). Выведите количество страниц.

## Вариант 7
Получите одну строку по id: $stmt->fetch(PDO::FETCH_OBJ). Обращение $row->field. Выведите поля объекта. Если запись не найдена — выведите «Нет данных».

## Вариант 8
Итерация по результату в цикле while: while ($row = $stmt->fetch(PDO::FETCH_ASSOC)) { ... }. Соберите массив $items[] = $row. Выведите count($items). Продемонстрируйте на запросе или симуляции.

## Вариант 9
Пагинация с защитой от отрицательной страницы: $page = max(1, (int)($_GET['page'] ?? 1)). Выведите «Текущая страница: {page}». Для $perPage=5 выведите offset для страниц 1, 2, 3.

## Вариант 10
fetch в разных режимах: один и тот же запрос вывести как FETCH_ASSOC, FETCH_NUM, FETCH_OBJ (три отдельных запроса или один и клонирование). Выведите по одной строке в каждом формате. Симулируйте одну строку [id=>1, name=>'Test'].

## Вариант 11
Функция getUsers(PDO $pdo, int $limit = 10): array. SELECT * FROM users LIMIT ?. execute([$limit]). return $stmt->fetchAll(PDO::FETCH_ASSOC). Вызовите с limit=5 и выведите количество элементов.

## Вариант 12
Получите результат mysqli в виде массива: $result->fetch_all(MYSQLI_ASSOC). Выведите все строки. Сравните с циклом fetch_assoc (результат тот же). Продемонстрируйте один из вариантов.

## Вариант 13
Вычислите offset для страницы: функция getOffset(int $page, int $perPage): int { return ($page - 1) * $perPage; }. Вызовите для (1, 10), (2, 10), (3, 5). Выведите результаты (0, 10, 10).

## Вариант 14
Проверка rowCount для SELECT: в PDO rowCount() для SELECT может быть ненадёжным в некоторых драйверах. Вместо этого используйте count($stmt->fetchAll()) для количества строк (учтите: курсор сбрасывается). Или выполните SELECT COUNT(*). Объясните в комментарии и выведите количество.

## Вариант 15
Получите первые 3 записи: LIMIT 3. fetchAll(). Выведите каждую запись в формате «id: {id}, name: {name}». Симулируйте массив из 3 элементов.

## Вариант 16
Формирование ссылок пагинации: для $totalPages=5 сгенерируйте массив ссылок [1=>'?page=1', 2=>'?page=2', ...]. Цикл for от 1 до totalPages. Выведите массив или HTML (на выбор).

## Вариант 17
Один запрос — несколько выборок: $stmt->fetch() для первой строки, затем снова fetch() для второй. Выведите обе строки. Продемонстрируйте на двух элементах (симуляция или реальный запрос с LIMIT 2).

## Вариант 18
Функция getPage(PDO $pdo, int $page, int $perPage): array. Вычислите offset, выполните SELECT ... LIMIT ? OFFSET ?, верните fetchAll(). Вызовите getPage($pdo, 2, 5) и выведите количество записей (симуляция: верните массив из 5 элементов).

## Вариант 19
Преобразование результата: получите массив ассоциативных массивов. Преобразуйте в массив с ключом по id: $byId = []; foreach ($rows as $r) { $byId[$r['id']] = $r; }. Выведите $byId для симулированных данных.

## Вариант 20
Проверка наличия записей: после execute() и fetch() проверьте, что $row не false. Если есть — выведите «Найдено: {name}», иначе «Запись не найдена». Продемонстрируйте оба варианта (подставьте несуществующий id для второго).
